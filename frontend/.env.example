# API is proxied from Vite as /api — no change needed for local dev.
# WebSocket server (Socket.io) runs on the backend port:
VITE_SOCKET_URL=http://127.0.0.1:5000

# Razorpay: open checkout on UPI with this VPA prefilled (collect / intent flow).
# Optional 10-digit mobile — some Razorpay UPI flows require contact prefill.
# VITE_RAZORPAY_PREFILL_VPA=bhattayush241-1@okaxis
# VITE_RAZORPAY_PREFILL_CONTACT=9876543210
